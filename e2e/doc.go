// Package e2e holds end-to-end tests that need a running docker daemon and the
// plugin installed in ~/.docker/cli-plugins. They are gated behind the e2e
// build tag: run them with `make e2e`.
package e2e
